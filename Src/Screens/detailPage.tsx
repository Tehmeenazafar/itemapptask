import React, { useState } from 'react'
import { View, Text, StyleSheet, Dimensions, Image, ScrollView, FlatList, TextInput } from 'react-native'
// interface Props {
//     visible: false
// }
 const App: React.FC = (props) => {
    
    return (
            <View>
                <Text>
                   Detail Page
                </Text>
            </View>
        )
    }
    export default App;
        const styles = StyleSheet.create({
            container:{
                flex:1,
            }
        })